/*
package com.sebn.backend.services;

import com.sebn.backend.dtos.RequestManagerDTO;
import com.sebn.backend.dtos.RespondManagerDTO;
import com.sebn.backend.entities.Manager;

import java.util.List;

public interface ManagerService {
    RespondManagerDTO saveManager(RequestManagerDTO manager);
    List<RespondManagerDTO> getAllManagers();
    RespondManagerDTO getManagerById(Long id);
    RespondManagerDTO updateManager(RequestManagerDTO manager, Long id);
    void deleteManager(Long id);
}
*/
